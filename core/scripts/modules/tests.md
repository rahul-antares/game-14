### Config
1. Reel Count, Grid Height
2. Reel Set size, No. of symbols(unique)


### Play
* Option/Mode which sends all the data in string format, just to match its true value across both envs/langs.
